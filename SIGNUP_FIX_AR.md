# إصلاح مشكلة التسجيل 🔧

## المشكلة
عند محاولة إنشاء حساب جديد، يظهر الخطأ: **"حدث خطأ أثناء إنشاء الحساب. حاول مرة أخرى."**

## السبب
المشكلة في سياسات Row Level Security (RLS) في قاعدة بيانات Supabase:
- عند التسجيل، المستخدم يكون غير مصادق عليه (anon role)
- السياسات الحالية لا تسمح للمستخدمين غير المصادقين بقراءة جدول `student_participants`
- هذا يمنع التحقق من وجود اسم المستخدم وإنشاء الحساب

## الحل

### الخطوة 1: تحديث سياسات RLS في Supabase
1. افتح لوحة تحكم Supabase: https://app.supabase.com
2. اذهب إلى SQL Editor
3. انسخ والصق محتوى ملف `fix_signup_rls.sql`
4. اضغط Run لتنفيذ الأوامر

### الخطوة 2: التحقق من الإصلاح
بعد تنفيذ SQL، جرب:
1. افتح صفحة التسجيل
2. أدخل اسم مستخدم جديد وكلمة مرور
3. اضغط "إنشاء حساب"
4. يجب أن يتم إنشاء الحساب بنجاح والانتقال إلى لوحة التحكم

## التغييرات المطبقة

### 1. في قاعدة البيانات (fix_signup_rls.sql)
- ✅ السماح بالتسجيل العام بدون مصادقة
- ✅ السماح بقراءة أسماء المستخدمين للتحقق من التكرار
- ✅ تحسين معالجة الأخطاء

### 2. في الكود (app/signup/actions.ts)
- ✅ استخدام `maybeSingle()` بدلاً من `single()` لتجنب الأخطاء
- ✅ إضافة معالجة أفضل للأخطاء مع try-catch
- ✅ تحسين رسائل الخطأ

### 3. في قاعدة البيانات الرئيسية (supabase_complete_setup.sql)
- ✅ تحديث السياسات الافتراضية

## اختبار الحل

### حالات الاختبار:
1. ✅ إنشاء حساب جديد بنجاح
2. ✅ رفض اسم مستخدم مكرر
3. ✅ التحقق من صحة كلمة المرور (8 أحرف على الأقل)
4. ✅ التحقق من صحة اسم المستخدم (3-30 حرف)
5. ✅ استخدام رموز الأدوار (اختياري)

## ملاحظات مهمة

### الأمان
- السياسات الجديدة تسمح بالقراءة العامة لجدول المستخدمين
- هذا آمن لأن:
  - كلمات المرور مشفرة (SHA-256)
  - لا يتم عرض معلومات حساسة
  - فقط للتحقق من أسماء المستخدمين

### الأداء
- استخدام `maybeSingle()` أفضل من `single()` لأنه لا يرمي خطأ إذا لم يجد نتائج
- معالجة الأخطاء محسّنة لتجنب الأخطاء غير المتوقعة

## إذا استمرت المشكلة

### تحقق من:
1. **متغيرات البيئة**: تأكد من أن `.env` يحتوي على:
   ```
   NEXT_PUBLIC_SUPABASE_URL=your_url
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your_key
   ```

2. **اتصال قاعدة البيانات**: تأكد من أن Supabase متصل

3. **Console Logs**: افتح Developer Tools في المتصفح وتحقق من الأخطاء

4. **Supabase Logs**: في لوحة تحكم Supabase، اذهب إلى Logs لرؤية الأخطاء

## الدعم
إذا واجهت أي مشاكل:
1. تحقق من ملف `SUPABASE_SETUP_GUIDE.md`
2. راجع ملف `AUTH_SETUP.md`
3. تحقق من console logs في المتصفح
