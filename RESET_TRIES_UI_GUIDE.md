# دليل واجهة إعادة تعيين المحاولات - Reset Tries UI Guide

## نظرة عامة على الواجهة الجديدة

تم استبدال رسالة المتصفح القديمة البسيطة بواجهة مستخدم مخصصة وجذابة تتكون من جزئين رئيسيين.

---

## الجزء الأول: النافذة المنبثقة (Modal)

### متى تظهر؟
عندما يحاول الطالب المشاركة بعد استنفاد جميع المحاولات المتاحة.

### المحتوى:

#### 1. الرأس (Header)
```
🎨 تدرج لوني: برتقالي → أحمر
⚠️ أيقونة تحذير متحركة (bounce animation)
📝 عنوان: "انتهت المحاولات المتاحة"
📊 معلومات: "لقد استخدمت جميع المحاولات (2 محاولات)"
```

#### 2. صندوق المعلومات (Info Box)
```
🔵 خلفية زرقاء فاتحة
ℹ️ أيقونة معلومات
📌 عنوان: "هل تريد المحاولة مرة أخرى؟"
📝 نص: "إذا كنت في مركز مصادر التعلم (LRC)، يمكنك طلب إعادة تعيين المحاولات من معلمك"
```

#### 3. خطوات إعادة التعيين
```
📋 عنوان: "خطوات إعادة تعيين المحاولات:"

① اذهب إلى معلمك في مركز مصادر التعلم (LRC)
② اطلب منه كود إعادة تعيين المحاولات
③ أدخل الكود في الصفحة السابقة وحاول مرة أخرى
```

#### 4. معلومات المعلم
```
🎨 تدرج لوني: بنفسجي → وردي
👨‍🏫 أيقونة معلم
📝 عنوان: "للمعلم فقط"
🔑 الكود: LRC@RESET (في صندوق أبيض بخط monospace)
```

#### 5. الأزرار
```
🏠 زر العودة للصفحة الرئيسية (تدرج أزرق)
❌ زر إغلاق (اختياري)
```

---

## الجزء الثاني: حقل إدخال الكود

### أين يظهر؟
في صفحة المشاركة نفسها، أسفل نموذج معلومات المشارك.

### المحتوى:

#### 1. الحاوية الرئيسية
```
🎨 خلفية: تدرج برتقالي فاتح
🔲 إطار: برتقالي متوسط (2px)
📐 زوايا مستديرة (rounded-2xl)
📏 حشوة داخلية: 24px
```

#### 2. الرأس
```
🔑 أيقونة مفتاح في دائرة برتقالية
📝 عنوان: "هل لديك كود إعادة تعيين من المعلم؟"
```

#### 3. النص التوضيحي
```
📝 "إذا كنت في مركز مصادر التعلم (LRC)، يمكنك الحصول على كود من معلمك لإعادة تعيين المحاولات"
```

#### 4. حقل الإدخال
```
📝 تسمية: "أدخل كود إعادة التعيين"
📥 حقل نصي:
   - Placeholder: "LRC@RESET"
   - خط monospace وعريض
   - محاذاة في المنتصف
   - تحويل تلقائي للأحرف الكبيرة
   - حد أقصى: 20 حرف
```

#### 5. زر التطبيق
```
✓ نص: "تطبيق"
🎨 تدرج أخضر (green-500 → green-600)
🔒 معطل إذا كان الحقل فارغاً
💫 تأثير hover: تدرج أغمق
```

#### 6. نص المساعدة
```
ℹ️ أيقونة معلومات صغيرة
📝 "الكود متاح فقط لدى معلم مركز مصادر التعلم (LRC)"
```

---

## تدفق المستخدم (User Flow)

### السيناريو الكامل:

```
1. الطالب يحاول المشاركة
   ↓
2. النظام يكتشف: لا محاولات متبقية
   ↓
3. تظهر النافذة المنبثقة الجميلة
   ↓
4. الطالب يقرأ التعليمات
   ↓
5. الطالب يذهب للمعلم في LRC
   ↓
6. المعلم يعطيه الكود: LRC@RESET
   ↓
7. الطالب يعود للصفحة
   ↓
8. يرى حقل إدخال الكود البارز
   ↓
9. يدخل الكود: LRC@RESET
   ↓
10. يضغط "تطبيق"
    ↓
11. النظام يتحقق من الكود
    ↓
12. رسالة نجاح: "✅ تم إعادة تعيين المحاولات بنجاح!"
    ↓
13. الصفحة تُحدّث تلقائياً
    ↓
14. الطالب يمكنه المشاركة مرة أخرى!
```

---

## الألوان المستخدمة

### النافذة المنبثقة:
- **الرأس:** `from-amber-500 via-orange-500 to-red-500`
- **صندوق المعلومات:** `bg-blue-50 border-blue-200`
- **الخطوات:** `bg-neutral-50`
- **معلومات المعلم:** `from-purple-50 to-pink-50 border-purple-200`
- **زر العودة:** `from-primary to-primary-dark`

### حقل الإدخال:
- **الحاوية:** `from-amber-50 to-orange-50 border-amber-300`
- **الأيقونة:** `bg-amber-500`
- **حقل الإدخال:** `border-amber-300 focus:border-amber-500`
- **زر التطبيق:** `from-green-500 to-green-600`

---

## الرسوم المتحركة (Animations)

### النافذة المنبثقة:
```css
@keyframes scale-in {
  from {
    opacity: 0;
    transform: scale(0.9);
  }
  to {
    opacity: 1;
    transform: scale(1);
  }
}
```

### الأيقونة:
```css
animate-bounce /* أيقونة التحذير */
```

### الخلفية:
```css
backdrop-blur-sm /* ضبابية الخلفية */
bg-black/60 /* تعتيم الخلفية */
```

---

## الاستجابة (Responsiveness)

### الأجهزة المحمولة:
- النافذة المنبثقة: `max-w-lg` (أقصى عرض 512px)
- حشوة خارجية: `p-4` (16px)
- النصوص تتكيف تلقائياً

### الأجهزة اللوحية والحواسيب:
- عرض مريح في المنتصف
- جميع العناصر واضحة ومقروءة

---

## إمكانية الوصول (Accessibility)

✓ ألوان متباينة للقراءة السهلة
✓ أحجام خطوط مناسبة
✓ أيقونات توضيحية
✓ نصوص واضحة بالعربية
✓ أزرار كبيرة سهلة الضغط
✓ تعطيل الأزرار عند عدم الحاجة

---

## مقارنة: قبل وبعد

### قبل (Old):
```
❌ رسالة متصفح بسيطة (alert)
❌ نص إنجليزي أو عربي بسيط
❌ زر OK فقط
❌ لا توجد تعليمات واضحة
❌ لا يوجد حقل لإدخال الكود
❌ تصميم غير جذاب
```

### بعد (New):
```
✅ نافذة منبثقة مخصصة جميلة
✅ تصميم احترافي بالألوان
✅ تعليمات واضحة خطوة بخطوة
✅ معلومات للمعلم والطالب
✅ حقل إدخال كود بارز
✅ رسوم متحركة سلسة
✅ تجربة مستخدم ممتازة
```

---

## الرسائل التوضيحية (Toast Messages)

### عند إدخال كود خاطئ:
```
🔴 "كود غير صحيح"
```

### عند النجاح:
```
🟢 "✅ تم إعادة تعيين المحاولات بنجاح!"
```

### عند حدوث خطأ:
```
🔴 "حدث خطأ أثناء إعادة تعيين المحاولات"
```

---

## ملاحظات للمطورين

### الملفات المعنية:
1. `components/OutOfTriesModal.tsx` - المكون الرئيسي للنافذة المنبثقة
2. `app/competition/[slug]/participate/ParticipationForm.tsx` - نموذج المشاركة المحدث

### الحالات (States):
```typescript
const [showOutOfTriesModal, setShowOutOfTriesModal] = useState(false)
const [resetCode, setResetCode] = useState('')
const [attemptInfo, setAttemptInfo] = useState<{
  canAttempt: boolean
  remainingAttempts: number
  maxAttempts: number
} | null>(null)
```

### الدوال الرئيسية:
```typescript
handleResetAttempts() // معالجة إعادة التعيين
checkAttempts() // التحقق من المحاولات المتبقية
```

---

**تم التطوير بواسطة:** يوسف محمد صبح
**التاريخ:** 2026
**الغرض:** تحسين تجربة المستخدم في منصة المسابقات
