# نظام الأسئلة - التوضيح

## الفرق بين مكتبة الأسئلة والأسئلة التدريبية

### 📚 الأسئلة التدريبية (Training Questions)
- **المسار**: `/dashboard/training-questions`
- **الخاصية**: `is_training = true`
- **الغرض**: أسئلة للتدريب فقط، متاحة للطلاب للممارسة
- **المسابقة**: غير مرتبطة بمسابقة محددة (`competition_id = null`)
- **الاستخدام**: يمكن للطلاب الوصول إليها في أي وقت للتدريب

### 🗄️ مكتبة الأسئلة (Question Bank)
- **المسار**: `/dashboard/question-bank`
- **الخاصية**: `is_training = false`
- **الغرض**: أسئلة مخزنة كمسودات، جاهزة للاستخدام في المسابقات
- **المسابقة**: يمكن ربطها بمسابقة محددة أو تركها بدون مسابقة
- **الاستخدام**: مدير المسابقة يختار منها عند إنشاء مسابقة جديدة

## التغييرات المطبقة

### 1. الفلاتر الافتراضية
```typescript
// الأسئلة التدريبية
is_training: 'true'

// مكتبة الأسئلة
is_training: 'false'
```

### 2. إزالة فلتر "التصنيف"
- تم إزالة فلتر التصنيف (مسابقة/تدريب) من واجهة الفلاتر
- كل صفحة الآن تعرض نوعها الخاص فقط
- تم إضافة توضيح في أعلى الفلاتر يشرح النوع المعروض

### 3. النموذج (Form)
- عند إنشاء سؤال جديد، يتم تعيين `is_training` تلقائياً حسب الصفحة:
  - في صفحة الأسئلة التدريبية: `is_training = true`
  - في صفحة مكتبة الأسئلة: `is_training = false`

### 4. زر "نقل للتدريب"
- يظهر فقط للأسئلة التي `is_training = false`
- يحول السؤال من مكتبة الأسئلة إلى الأسئلة التدريبية
- يزيل ارتباط السؤال بأي مسابقة

## مثال عملي

### سيناريو 1: إنشاء سؤال تدريبي
1. اذهب إلى `/dashboard/training-questions`
2. اضغط "إضافة سؤال"
3. املأ البيانات
4. السؤال سيُنشأ بـ `is_training = true` تلقائياً
5. السؤال متاح للطلاب للتدريب فوراً

### سيناريو 2: إنشاء سؤال في المكتبة
1. اذهب إلى `/dashboard/question-bank`
2. اضغط "إضافة سؤال"
3. املأ البيانات
4. السؤال سيُنشأ بـ `is_training = false` تلقائياً
5. السؤال مخزن في المكتبة، جاهز للاستخدام في مسابقة

### سيناريو 3: نقل سؤال من المكتبة للتدريب
1. اذهب إلى `/dashboard/question-bank`
2. اختر سؤال
3. اضغط "نقل للتدريب"
4. السؤال ينتقل إلى الأسئلة التدريبية
5. يصبح متاحاً للطلاب للتدريب

## الخلاصة

الآن الصفحتان منفصلتان تماماً:
- ✅ **الأسئلة التدريبية**: تعرض فقط `is_training = true`
- ✅ **مكتبة الأسئلة**: تعرض فقط `is_training = false`
- ✅ لا يوجد تداخل بين الصفحتين
- ✅ كل صفحة لها غرض واضح ومحدد
