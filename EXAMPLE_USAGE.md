# Example Usage - أمثلة الاستخدام

## Scenario 1: Student Runs Out of Tries

### What the Student Sees:

#### Step 1: Beautiful Modal Appears
```
┌─────────────────────────────────────────────┐
│  🎨 Gradient Header (Orange → Red)          │
│                                             │
│           ⚠️ (bouncing)                     │
│                                             │
│      انتهت المحاولات المتاحة                │
│   لقد استخدمت جميع المحاولات (2 محاولات)   │
└─────────────────────────────────────────────┘
│                                             │
│  ┌───────────────────────────────────────┐ │
│  │ ℹ️  هل تريد المحاولة مرة أخرى؟      │ │
│  │                                       │ │
│  │  إذا كنت في مركز مصادر التعلم (LRC)  │ │
│  │  يمكنك طلب إعادة تعيين المحاولات     │ │
│  │  من معلمك                            │ │
│  └───────────────────────────────────────┘ │
│                                             │
│  📋 خطوات إعادة تعيين المحاولات:           │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │ ① اذهب إلى معلمك في LRC            │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │ ② اطلب منه كود إعادة تعيين         │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │ ③ أدخل الكود وحاول مرة أخرى       │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌───────────────────────────────────────┐ │
│  │ 👨‍🏫 للمعلم فقط                       │ │
│  │                                       │ │
│  │  كود إعادة التعيين:                  │ │
│  │  ┌─────────────┐                     │ │
│  │  │   12311     │                     │ │
│  │  └─────────────┘                     │ │
│  └───────────────────────────────────────┘ │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │   العودة للصفحة الرئيسية  🏠       │   │
│  └─────────────────────────────────────┘   │
└─────────────────────────────────────────────┘
```

#### Step 2: Student Goes to Teacher
```
Student: "أستاذ، انتهت محاولاتي في المسابقة"
Teacher: "حسناً، هذا هو كود إعادة التعيين: 12311"
Student: "شكراً أستاذ!"
```

#### Step 3: Student Returns to Page
```
┌─────────────────────────────────────────────┐
│  معلومات المشارك                            │
│                                             │
│  [الاسم الأول]  [اسم الأب]  [اسم العائلة]  │
│  [الصف]        [الفصل]                      │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │ ابدأ الإجابة على الأسئلة           │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │
│                                             │
│  🎨 Amber/Orange Gradient Box               │
│  ┌─────────────────────────────────────┐   │
│  │ 🔑 هل لديك كود إعادة تعيين من      │   │
│  │    المعلم؟                          │   │
│  │                                     │   │
│  │ إذا كنت في مركز مصادر التعلم (LRC) │   │
│  │ يمكنك الحصول على كود من معلمك      │   │
│  │                                     │   │
│  │ أدخل كود إعادة التعيين              │   │
│  │ ┌─────────────┐  ┌──────────┐      │   │
│  │ │   12311     │  │ ✓ تطبيق │      │   │
│  │ └─────────────┘  └──────────┘      │   │
│  │                                     │   │
│  │ ℹ️ الكود متاح فقط لدى معلم LRC     │   │
│  └─────────────────────────────────────┘   │
└─────────────────────────────────────────────┘
```

#### Step 4: Student Enters Code
```
Input Field: [1][2][3][1][1]
```

#### Step 5: Success!
```
┌─────────────────────────────────────────┐
│  ✅ تم إعادة تعيين المحاولات بنجاح!    │
└─────────────────────────────────────────┘

[Page reloads automatically after 1.5 seconds]

Student can now participate again! 🎉
```

---

## Scenario 2: Wrong Code Entered

```
Student enters: "WRONG_CODE"
                     ↓
┌─────────────────────────────────────────┐
│  ❌ كود غير صحيح                       │
└─────────────────────────────────────────┘

Student can try again
```

---

## Scenario 3: Teacher Workflow

### Teacher's Checklist:
```
□ Student approaches in LRC
□ Verify student identity
□ Check if student genuinely needs reset
□ Provide code: LRC@RESET
□ Ensure student understands how to use it
```

### Teacher's Script:
```
Teacher: "مرحباً، كيف يمكنني مساعدتك؟"
Student: "انتهت محاولاتي في المسابقة"
Teacher: "حسناً، سأعطيك كود إعادة التعيين"
Teacher: "الكود هو: 12311"
Teacher: "اكتبه في الحقل الموجود في الصفحة"
Student: "شكراً أستاذ!"
```

---

## Code Examples

### 1. Using the Modal Component

```typescript
import OutOfTriesModal from '@/components/OutOfTriesModal'

// In your component
const [showModal, setShowModal] = useState(false)

// Show modal when out of tries
if (attemptInfo && !attemptInfo.canAttempt) {
  return <OutOfTriesModal maxAttempts={attemptInfo.maxAttempts} />
}
```

### 2. Reset Code Input

```typescript
const [resetCode, setResetCode] = useState('')

const handleResetAttempts = async () => {
  if (resetCode !== '12311') {
    showToast('كود غير صحيح', 'error')
    return
  }
  
  // Call API to reset
  const response = await fetch('/api/attempts/reset', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      competitionId: competition.id,
      deviceFingerprint: getOrCreateFingerprint(),
      resetCode: resetCode,
    }),
  })
  
  if (response.ok) {
    showToast('✅ تم إعادة تعيين المحاولات بنجاح!', 'success')
    setTimeout(() => window.location.reload(), 1500)
  }
}
```

### 3. API Call

```typescript
// POST /api/attempts/reset
{
  "competitionId": "123e4567-e89b-12d3-a456-426614174000",
  "deviceFingerprint": "abc123def456...",
  "resetCode": "12311"
}

// Response
{
  "canAttempt": true,
  "remainingAttempts": 2,
  "maxAttempts": 2,
  "message": "Attempts reset successfully"
}
```

---

## Visual States

### State 1: Normal (Has Attempts)
```
✅ Can participate
📊 Remaining: 2/2 attempts
🎯 No modal shown
🔓 Form is accessible
```

### State 2: Warning (Last Attempt)
```
⚠️ Warning message before submit
📊 Remaining: 1/2 attempts
💬 Confirmation dialog
🎯 Can still participate
```

### State 3: Out of Tries
```
🚫 Cannot participate
📊 Remaining: 0/2 attempts
🎨 Beautiful modal appears
🔑 Reset code input shown
```

### State 4: After Reset
```
✅ Can participate again
📊 Remaining: 2/2 attempts
🎉 Success message
🔄 Page reloads
```

---

## Color Coding

### Modal Colors:
```css
Header:     🟠 Amber → Orange → Red
Info Box:   🔵 Blue (light)
Steps:      ⚪ Neutral (light gray)
Teacher:    🟣 Purple → Pink
Button:     🔵 Primary gradient
```

### Input Colors:
```css
Container:  🟠 Amber/Orange gradient
Icon:       🟠 Amber solid
Input:      ⚪ White with amber border
Button:     🟢 Green gradient
Helper:     🟠 Amber text
```

---

## Responsive Behavior

### Mobile (< 640px):
```
┌─────────────┐
│   Modal     │
│  (Centered) │
│             │
│  Full width │
│  padding    │
└─────────────┘
```

### Tablet (640px - 1024px):
```
┌───────────────────┐
│      Modal        │
│   (max-w-lg)      │
│                   │
│  Comfortable      │
│  spacing          │
└───────────────────┘
```

### Desktop (> 1024px):
```
┌─────────────────────────┐
│        Modal            │
│     (max-w-lg)          │
│                         │
│   Centered with         │
│   backdrop blur         │
└─────────────────────────┘
```

---

## Animation Timeline

```
0ms:    Modal appears (opacity: 0, scale: 0.9)
        ↓
300ms:  Modal fully visible (opacity: 1, scale: 1)
        ↓
        Warning icon bounces continuously
        ↓
User interacts...
        ↓
Click "Apply"
        ↓
1500ms: Success toast appears
        ↓
3000ms: Page reloads
```

---

## Error Handling

### Scenario A: Network Error
```
❌ "حدث خطأ أثناء إعادة تعيين المحاولات"
→ User can try again
```

### Scenario B: Invalid Code
```
❌ "كود غير صحيح"
→ User can enter correct code
```

### Scenario C: Competition Not Found
```
❌ "المسابقة غير موجودة"
→ Redirect to home
```

---

## Success Metrics

### User Experience:
- ✅ Clear instructions
- ✅ Beautiful design
- ✅ Easy to use
- ✅ Fast response
- ✅ Helpful feedback

### Technical:
- ✅ Type-safe
- ✅ Responsive
- ✅ Accessible
- ✅ Performant
- ✅ Secure

---

**Project:** Omani Encyclopedia Competition
**Developer:** Youssef Mohamed Sobh
**Year:** 2026
**Status:** ✅ Production Ready
